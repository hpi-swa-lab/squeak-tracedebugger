documentation
proxyHacks

	 "Integration of proxies in the base system is still hacky since they swallow any side effects. Should we eliminate #useProxiesAlways so that a simple #copy often suffices? Should we explicitly allow side effects to all arguments passed to a proxy in a message?"